# Development and CI dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.3.4
pytest-cov==7.1.0
ruff==0.16.4
mypy==1.14.1
# respx stubs httpx at the transport layer, which lets the connector tests
# assert against real response payloads without touching the network.
respx==0.23.1
